{% extends 'base.html.twig' %}

{% block body %}
    <h1>SurveyResponse</h1>

    <table>
        <tbody>
            <tr>
                <th>Value</th>
                <td>{{ surveyResponse.value }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ surveyResponse.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('surveyresponse_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('surveyresponse_edit', { 'id': surveyResponse.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
