{% extends 'base.html.twig' %}

{% block body %}
    <h1>Survey list</h1>

    <table>
        <thead>
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>Createddate</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for survey in surveys %}
            <tr>
                <td><a href="{{ path('survey_show', { 'id': survey.id }) }}">{{ survey.title }}</a></td>
                <td>{{ survey.description }}</td>
                <td>{% if survey.createddate %}{{ survey.createddate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ survey.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('survey_show', { 'id': survey.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('survey_edit', { 'id': survey.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('survey_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
